<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mansourahmed.android.chat" >

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".client.ChatActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".client.ConnectionActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".server.ServerStartActivity"
            android:label="@string/title_activity_server_start" >
        </activity>
        <activity
            android:name=".client.LocalServerConnectionActivity"
            android:label="@string/title_activity_local_server_connection" >
        </activity>
    </application>

</manifest>
